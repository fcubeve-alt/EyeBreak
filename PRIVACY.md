# EyeBreak 隐私说明

最后更新：2026-09-11

EyeBreak 不联网、没有账号、没有服务器。**所有数据都留在你的设备上。**

---

## 读取哪些数据

EyeBreak 通过 Apple 的 Family Controls / DeviceActivity 框架，
只读取**聚合的屏幕活动时长**：

| 读取项 | 说明 |
|---|---|
| 累计用屏时长 | 某时间区间内屏幕保持开启的总时长 |
| 最长连续活动会话 | 系统给出的片段内最长一次连续活动时长 |
| 活动片段的时间区间 | 片段的起止时间 |
| 系统数据更新时间 | 系统最后一次刷新活动数据的时刻 |

## 不读取哪些数据

- ❌ **不读取你用了哪些 App** — 授权时使用 `.individual` 模式，
  不请求 `approvedWithDataAccess`，因此拿不到具体的 App 名称或网站地址
- ❌ 不读取你的浏览记录、输入内容、截图或屏幕内容
- ❌ 不读取通讯录、位置、照片、麦克风、摄像头
- ❌ 不读取任何可识别你个人身份的信息

产品本身也不需要知道你在用微信还是别的什么，只需要知道
**设备近期是否处于持续用屏状态**。

---

## 数据存在哪里

保存在设备本地的 App Group 容器
（`group.com.eyebreak.shared` 的 `UserDefaults`）中，
供主 App 和几个扩展进程共享读写。

保存的内容包括：检测里程碑时间戳、使用密度、当日累计时长、
触发层级、冷却截止时间、以及当日完成/跳过/稍后的次数。

**这些数据从不离开设备。** App 没有任何网络请求代码，
没有分析 SDK，没有崩溃上报，没有广告。

---

## 数据保留多久

- 检测状态和当日计数**每天自动清零**
- 卸载 App 时，App Group 容器随之被系统删除，所有数据一并清除

## 如何删除数据

- **清除检测数据**：在 App 内点「停止监测」，或直接卸载 App
- **撤销权限**：设置 → 屏幕使用时间 → 取消对 EyeBreak 的授权

---

## 关于 Shield（应用遮罩）

触发提醒时，EyeBreak 会用系统的 Shield 功能临时遮住 App。
这是 iOS 提供的系统能力，EyeBreak 只是请求系统显示遮罩，
**无法看到、也无法记录被遮住的是什么 App**。

遮罩始终可由你关闭，并且有到期自动解除的兜底机制（见 README）。

---

## 系统日志

诊断日志通过 OSLog 写入系统日志，动态内容标记为 `.private`，
在未连接调试器时会被系统脱敏。日志不会被上传。

---

## 第三方服务

无。没有使用任何第三方 SDK、分析服务、云存储或后端 API。

---

## 联系

问题或疑问请在 GitHub 仓库提 issue。
